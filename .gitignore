node_modules/
.wrangler/
.dev.vars
.env
.env.local
dist/
.DS_Store
*.log
logs/

# Claude Code local config
.claude/
.gstack/

# Tasker project exports — contain the live API bearer token; never commit.
tasker/
*.prj.xml

# Web Push VAPID key backup — private key, never commit.
.vapid-prod.env

# 1001tracklists account credentials for the NAS forwarder — never commit.
.1001tl-accounts.env
